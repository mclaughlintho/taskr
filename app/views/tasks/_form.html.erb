<%= form_for(@task) do |f| %>

<%= f.label :title %>
<span id="new-task"><%= f.text_field :title, class:"form-control" %></span>

<span id="create-task"><%= f.submit "Create Task", class:"btn btn-primary" %></span>

<% end %>